# Project-alpha

## Branch Guide

- `main`: Full source code (`src`, configs, components, and all development files).
- `gh-pages`: Production build output only (files from `dist` used by GitHub Pages hosting).

If you want to review or download all project files, switch to the `main` branch.
If you only want the live hosted site artifacts, check `gh-pages`.
